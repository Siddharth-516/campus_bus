Place your app logo at `assets/logo.png` (PNG recommended).

Recommended size: 500×500 or 1024×1024.

After adding the file, run:

```bash
flutter pub get
flutter pub run flutter_launcher_icons:main
```

This will generate launcher icons for Android and iOS using the provided image.
